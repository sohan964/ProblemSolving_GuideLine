#include <bits/stdc++.h>
#include<cmath>

     
using namespace std;
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);


int main(){
        optimize();
        
        set<string, greater<string>> s;//it's will print decrsing order
        s.insert("sahin");
        s.insert("shaon");
        s.insert("sohan");
        s.insert("sohan");
        s.insert("sahin");
        s.insert("shaon");
        for(auto u:s) cout<<u<<" ";//it's will print luckgo graphacali larger to smaller
        
        cout<<endl;
     
}
